[build]
  functions = "netlify_functions"  # Directory where your functions reside
  publish = "/"                   # Since you have no static files
  command = "pip install -r requirements.txt"  # To install the dependencies

[[redirects]]
  from = "/"
  to = "/.netlify/functions/netlify_functions/flask_app"  # Correct path to the function
  status = 200
  force = true
